import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import ackoxIcons from '@acko-components/icons/dist/ackox/font/ackox.json';

# Icon font

<Meta title="Icons / Icon font" />

<Preview>
  {Object.keys(ackoxIcons).map((icon) => (
    <span
      key={icon}
      aria-label={icon}
      className={`acx-${icon}`}
      style={{ fontSize: 32 }}></span>
  ))}
</Preview>
