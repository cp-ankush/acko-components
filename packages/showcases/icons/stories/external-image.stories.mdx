import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import ackoxIcons from '@acko-components/icons/dist/ackox/font/ackox.json';

# External image

<Meta title="Icons / External image" />

<Preview>
  {Object.keys(ackoxIcons).map((icon) => (
    <img
      key={icon}
      src={require(`@acko-components/icons/dist/ackox/svg/${icon}.svg`)}
      alt={icon}
      width="32"
      height="32"
    />
  ))}
</Preview>
