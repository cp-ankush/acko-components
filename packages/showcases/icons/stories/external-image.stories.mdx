import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import vitamixIcons from '@acko-components/icons/dist/vitamix/font/vitamix.json';

# External image

<Meta title="Icons / External image" />

<Preview>
  {Object.keys(vitamixIcons).map((icon) => (
    <img
      key={icon}
      src={require(`@acko-components/icons/dist/vitamix/svg/${icon}.svg`)}
      alt={icon}
      width="32"
      height="32"
    />
  ))}
</Preview>
